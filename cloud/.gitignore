.venv/
.claude/
CLAUDE.md
__pycache__/
*.pyc
.dev-pids

# tools/fix_ur3_pose.py 가 남기는 원본 백업 — git 이 이미 원본을 들고 있다.
*.glb.bak

# 방문 집계(visit_log.py)는 서버가 돌 때마다 쌓이는 런타임 로그다 — 누가 앱을 띄우든
# 매번 변경으로 잡혀 커밋·머지에 끼어든다. 퀴즈 결과(TEST_RESULT)처럼 보존할 자료가
# 아니라 이 PC 의 방문 집계이므로 추적하지 않는다. 파일은 디스크에 그대로 쌓인다.
VISIT_LOG/
